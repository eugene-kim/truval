/**
 * This method takes in normalized GraphQL results and fills in the store appropriately.
 */
export default const hydrateStore = (normalizedData, store) => {

};

const hydrateEntities = (normalizedData, store) {
  
};