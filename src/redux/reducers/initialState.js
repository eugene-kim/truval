import { Map } from 'immutable';


export default {
  entities: {
    user: {
      entities: Map(),
    },

    session: {
      entities: Map(),
    },

    activity: {
      entities: Map(),
    },

    category: {
      entities: Map(),
    },
  },
};